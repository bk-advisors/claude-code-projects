# claude-code-test

Minimal workspace for testing coding with Claude/assistant.

Quick start (Windows, Python):

1. Open a terminal in this folder.
2. (Optional) Create a venv: `python -m venv .venv` then activate it.
3. Install deps: `pip install -r requirements.txt` (none required right now).
4. Run the sample: `python test.py`
